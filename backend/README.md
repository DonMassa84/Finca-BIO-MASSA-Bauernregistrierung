# 🌱 Finca BIO MASSA – Bauernregistrierung (GitHub Pages, offline)

Frontend-App zur Registrierung von Kakaobauern (ohne Server, offlinefähig via PWA).  
- Felder: Name, Nachname, Identität, Geburtsort/-datum, Familienstand, Kinder, Mitglieds-Nr., kg/Monat, Telefon, Zweitkontakt, Foto, Notizen  
- Suche, Bearbeiten/Löschen, **CSV/JSON Export & Import**, **Foto-Upload**  
- Sprachen: Deutsch · Français · Español  
- Daten bleiben lokal im Browser (LocalStorage). Späterer API-Anschluss möglich.

## Start
1) GitHub → **Settings → Pages** → Branch `main`, Folder `/docs`.  
2) Seite aufrufen: `https://<dein-user>.github.io/<repo>/`

## Backend (optional)
Wenn später gewünscht, kann eine API (Render / Fly.io) unter `/api/farmers` angeschlossen werden. Das Frontend bleibt unverändert.

---
🇫🇷 *Application hors-ligne (PWA) pour l’enregistrement des producteurs de cacao. Export CSV/JSON, photo, recherche, multilingue (DE/FR/ES).*
